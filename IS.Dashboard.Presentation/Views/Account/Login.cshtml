@model dashboard.Models.LoginModel

@{
    ViewBag.Title = "登录";
    Layout = null;
}

@*<hgroup class="title">
    <h1>@ViewBag.Title.</h1>
</hgroup>

<section id="loginForm">
<h2>使用本地帐户登录。</h2>
@using (Html.BeginForm(new { ReturnUrl = ViewBag.ReturnUrl })) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>“登录”表单</legend>
        <ol>
            <li>
                @Html.LabelFor(m => m.UserName)
                @Html.TextBoxFor(m => m.UserName)
                @Html.ValidationMessageFor(m => m.UserName)
            </li>
            <li>
                @Html.LabelFor(m => m.Password)
                @Html.PasswordFor(m => m.Password)
                @Html.ValidationMessageFor(m => m.Password)
            </li>
            <li>
                @Html.CheckBoxFor(m => m.RememberMe)
                @Html.LabelFor(m => m.RememberMe, new { @class = "checkbox" })
            </li>
        </ol>
        <input type="submit" value="登录" />
    </fieldset>
    <p>
        @Html.ActionLink("注册", "Register") 如果你没有帐户。
    </p>
}
</section>

<section class="social" id="socialLoginForm">
    <h2>使用其他服务登录。</h2>
    @Html.Action("ExternalLoginsList", new { ReturnUrl = ViewBag.ReturnUrl })
</section>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}*@

<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <link href="../../css/login.css" rel='stylesheet' type='text/css' />
    <link href="../../Content/bootstrap.css" rel='stylesheet' type='text/css' />
    <link href="../../Content/bootstrap.min.css" rel='stylesheet' type='text/css' />
    <script src="../../Scripts/jquery-1.10.2.js"></script>
    <script src="../../Scripts/js/bootstrap.js"></script>
    <script src="../../Scripts/js/bootstrap.min.js"></script>
    <script src="../../Scripts/js/copyright.js"></script>
    <script src="../../Scripts/js/focus.js"></script>
    <script src="../../Scripts/js/ckLg.js"></script>
    <title>Login</title>
</head>
<body>
    <!--公司logo-->
    <div class="logo">
        <img src="../../Images/cip-hpe.png" />
    </div>
    <!--登录部分-->
    <div class="login">
        <div class="login-top">
            <!--系统名称-->
            <h1>Dashboard</h1>
            <!--登录表单-->
            <form class="lgForm" autocomplete="on">
                <!--用户名-->
                <input type="text" placeholder="Username" id="name" onfocus="fs(this.id);" onblur="bl(this.id)" required="required">
                <!--密码-->
                <input type="password" placeholder="Password" id="psw" onfocus="fs(this.id);" onblur="bl(this.id)" autocomplete="off" required="required">
            </form>
            <div class="remember">
                <!--是否记住密码-->
                <div class="checkbox">
                    <label>
                        <input type="checkbox" class="ckbox" />
                        <font class="ft">remember me &nbsp;</font>
                    </label>
                </div>
                <!--登录按钮-->
                <input type="submit" value="Login" onclick="ckLogin();">
            </div>
        </div>
        <!--忘记密码-->
        <div class="login-bottom">
            <h3>forgot Password &nbsp;<a href="#">Click</a>&nbsp Here</h3>
        </div>
    </div>
    <!--版权信息-->
    <div class="copyright">
        <p>Copyright &copy; 2016.<font class="company"></font> All rights reserved.</p>
    </div>
</body>
</html>